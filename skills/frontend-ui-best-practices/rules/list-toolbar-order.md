---
title: 列表工具栏顺序：搜索 → 筛选 → 刷新
impact: LOW
tags: list, toolbar, consistency
---

## 列表工具栏顺序：搜索 → 筛选 → 刷新

固定顺序：搜索框在最前，筛选项居中，纯图标的刷新按钮在最后。

顺序固定后，用户在不同列表页之间切换时不需要重新寻找控件位置。刷新放最后且只用图标，是因为它是最低频的操作，不该占据视觉主位。

表格行内的操作单元格保持**风格统一**：要么全用图标，要么全用文字，不要图标文字混排——混排会让同一列的行高与视觉重量不一致。
